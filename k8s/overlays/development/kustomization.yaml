# Kustomize overlay for development environment
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: omni-ava-development

namespace: omni-ava-dev

commonLabels:
  environment: development

resources:
  - ../../base

namePrefix: dev-

patches:
  # Reduce replicas for development
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-api
      spec:
        replicas: 1
    target:
      kind: Deployment
      name: omni-ava-api

  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-engine
      spec:
        replicas: 1
    target:
      kind: Deployment
      name: omni-ava-engine

  # Reduce resource limits
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-api
      spec:
        template:
          spec:
            containers:
              - name: api
                resources:
                  requests:
                    cpu: "100m"
                    memory: "256Mi"
                  limits:
                    cpu: "500m"
                    memory: "1Gi"
    target:
      kind: Deployment
      name: omni-ava-api

  # Disable HPA in development
  - patch: |-
      apiVersion: autoscaling/v2
      kind: HorizontalPodAutoscaler
      metadata:
        name: omni-ava-api-hpa
      spec:
        minReplicas: 1
        maxReplicas: 2
    target:
      kind: HorizontalPodAutoscaler
      name: omni-ava-api-hpa

  # Reduce storage
  - patch: |-
      apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: omni-ava-data
      spec:
        resources:
          requests:
            storage: 5Gi
    target:
      kind: PersistentVolumeClaim
      name: omni-ava-data

configMapGenerator:
  - name: omni-ava-config
    behavior: merge
    literals:
      - OMNI_LOG_LEVEL=DEBUG
      - OMNI_ENABLE_PROFILING=true
      - OMNI_API_WORKERS=1
