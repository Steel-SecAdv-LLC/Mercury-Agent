# Kustomize overlay for production environment
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: omni-ava-production

namespace: omni-ava

commonLabels:
  environment: production

resources:
  - ../../base

namePrefix: prod-

patches:
  # Production replicas
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-api
      spec:
        replicas: 3
    target:
      kind: Deployment
      name: omni-ava-api

  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-engine
      spec:
        replicas: 2
    target:
      kind: Deployment
      name: omni-ava-engine

  # Production resource allocation
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-api
      spec:
        template:
          spec:
            containers:
              - name: api
                resources:
                  requests:
                    cpu: "500m"
                    memory: "1Gi"
                  limits:
                    cpu: "2"
                    memory: "4Gi"
    target:
      kind: Deployment
      name: omni-ava-api

  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: omni-ava-engine
      spec:
        template:
          spec:
            containers:
              - name: engine
                resources:
                  requests:
                    cpu: "1"
                    memory: "4Gi"
                  limits:
                    cpu: "4"
                    memory: "16Gi"
    target:
      kind: Deployment
      name: omni-ava-engine

  # Production HPA settings
  - patch: |-
      apiVersion: autoscaling/v2
      kind: HorizontalPodAutoscaler
      metadata:
        name: omni-ava-api-hpa
      spec:
        minReplicas: 3
        maxReplicas: 20
    target:
      kind: HorizontalPodAutoscaler
      name: omni-ava-api-hpa

  - patch: |-
      apiVersion: autoscaling/v2
      kind: HorizontalPodAutoscaler
      metadata:
        name: omni-ava-engine-hpa
      spec:
        minReplicas: 2
        maxReplicas: 10
    target:
      kind: HorizontalPodAutoscaler
      name: omni-ava-engine-hpa

  # Production storage
  - patch: |-
      apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: omni-ava-data
      spec:
        resources:
          requests:
            storage: 50Gi
    target:
      kind: PersistentVolumeClaim
      name: omni-ava-data

  - patch: |-
      apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: omni-ava-models
      spec:
        resources:
          requests:
            storage: 100Gi
    target:
      kind: PersistentVolumeClaim
      name: omni-ava-models

configMapGenerator:
  - name: omni-ava-config
    behavior: merge
    literals:
      - OMNI_LOG_LEVEL=WARNING
      - OMNI_ENABLE_PROFILING=false
      - OMNI_API_WORKERS=4
      - OMNI_ETHICS_STRICT_MODE=true
