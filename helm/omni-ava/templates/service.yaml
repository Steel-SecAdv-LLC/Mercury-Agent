{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "omni-ava.fullname" . }}-api
  namespace: {{ include "omni-ava.namespace" . }}
  labels:
    {{- include "omni-ava.api.labels" . | nindent 4 }}
  {{- with .Values.api.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.api.service.type }}
  selector:
    {{- include "omni-ava.api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.service.targetPort }}
      protocol: TCP
    {{- if .Values.metrics.enabled }}
    - name: metrics
      port: {{ .Values.api.service.metricsPort }}
      targetPort: {{ .Values.metrics.port }}
      protocol: TCP
    {{- end }}
{{- end }}
---
{{- if .Values.engine.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "omni-ava.fullname" . }}-engine
  namespace: {{ include "omni-ava.namespace" . }}
  labels:
    {{- include "omni-ava.engine.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "omni-ava.engine.selectorLabels" . | nindent 4 }}
  ports:
    {{- if .Values.metrics.enabled }}
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: {{ .Values.metrics.port }}
      protocol: TCP
    {{- end }}
{{- end }}
