# ML dependencies for Mercury-Agent (heavy ML stack)
# These are the heavy dependencies for full ML functionality

torch>=2.2.0
pytorch-lightning>=2.0.0
deepface>=0.0.79
opencv-python>=4.8.0
pillow>=10.4.0  # Security: CVE patches for image processing vulnerabilities

# Visual Anomaly Detection (PatchCore, PaDiM, STFPM, etc.)
torchvision>=0.17.0
timm>=0.9.0  # PyTorch Image Models for backbones

# Vision-Language Models (AnyAnomaly, LAVAD)
transformers>=4.36.0
accelerate>=0.25.0

# Foundation Model Adapters
stumpy>=1.12.0  # Matrix Profile
nixtla>=0.5.0  # TimeGPT API client

# Optional: Chronos requires separate install
# pip install git+https://github.com/amazon-science/chronos-forecasting.git

# Optional: FAISS for efficient nearest neighbor (PatchCore)
# faiss-cpu>=1.7.4 or faiss-gpu>=1.7.4

# API dependencies for integration tests
fastapi>=0.109.0
httpx>=0.25.0

# Testing dependencies for ML tests
hypothesis>=6.0.0  # Property-based testing for benevolence/sigma_sacred
